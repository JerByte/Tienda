import { Component } from "react";
import styles from './styles.css'

class Title extends Component {
    render() {
        return(
            <h1 className="title" style={styles}>Tienda</h1>
        )
    }
}

export default Title